## 0.5.8 — 2026-09-10

输入区双写：同一份主题在 0.1.1 与 0.1.5 内核下都生效——主题与内核版本解耦，可独立发布。

**背景**
- 内核 0.1.5 把输入区从 `<textarea>` 重构成 `div[contenteditable][role=textbox]`；
  实时 DOM 实测（Hermetic 起内核 + headless Chrome 取锚点清单，新旧两代对比）确认映射：
  `data-input-backdrop → data-composer-input`、`data-input-mirror → data-composer-placeholder`、
  `[data-composer-seat] textarea → [data-composer-input]`。

**改动**
- `theme-core.ts`：写作区/占位/聚焦三条规则并列新旧两代选择器。只改新选择器会让 0.1.1 上的
  输入框立刻失去主题化（版本绑定）；双写则两边都成立，因此这次改动不必与内核升级同步。

**验证**
- `render.composer-theme` 探针（dsh-desktop `verify:render` 新增）：0.1.1 命中
  `[data-composer-seat] textarea`、0.1.5 命中 `[data-composer-input]`，两者写作区底色均为
  PALIS 黑 `rgb(10,10,10)`、字体均为 JetBrains Mono。
- 契约 `theme.dom-anchors` 改为 either/or 语义（双写组任一存在即通过）：两代内核各跑一遍
  均 12 项全命中；渲染不变量四项全绿。

## 0.5.7 — 2026-09-09

声纳 ping 环变形修复：扩散环从 CSS `transform:scale` 关键帧动画改为 JS 逐帧写 `width/height/margin/opacity`——圆形动效零合成层的最后一块拼图（v0.5.5 月盘同法典）。

**根因**
- 用户报「月盘依旧不圆，声纳波纹也不圆」，但对当前截图逐像素测量：月盘轮廓、r=430 点环、蓝色虚线环、月坑、粒子云全部几何正圆——静态渲染已健康，残留的是**动态观感**。
- 全主题里唯一还在用合成层 transform 做圆形缩放的元素 = ping 环（`palis-sonar-ping` 的 `scale(.05→1)`）。分数 DPR（用户机 2.73）下合成层位图逐帧缩放，周向采样不均，运动中读作"不圆"。

**改动**
- `theme-core.ts`：`.palis-sonar i` 删除 `transform:scale(.05)` 与 `animation:palis-sonar-ping`（含错相 delay 与活动态 duration 规则），`@keyframes palis-sonar-ping` 退役；CSS 只留基态尺寸与描边（活动态描边提亮保留）。
- `client/index.ts`：新增 `PING_EASE`（cubic-bezier(.17,.67,.35,1) 二分求解器）；`orbitFrame` 逐帧驱动三环——展开曲线、1/3 周期错相、透明度包络（0→pk@9%→0.4·pk@62%→0）、活动态变速（6.4s/.42 ↔ 4s/.60）全部与原 keyframes 同参复刻。
- ping 重尺寸机制（§25 阈值门 + 冻结窗 + v0.5.3 不可见窗口落笔）保留，落笔对象从元素样式改为 `pingBaseDs[j]` 基准径数字（三环错相下不存在全环同隐窗口，故各环持有自己的基准径，各自等自己的不可见窗口换径）。

**验证**
- 测试内核 + Electron 探针（桌面版同二进制）：三环宽度构造上恒等（width≡height）、逐帧 opacity 包络与原 keyframes 一致、重尺寸仍落在 opacity≤0.02 窗口。
- 回归全绿：verify-flat / verify-craters / verify-glyphs / verify-ping-invisible。

**交付注记**
- 宿主内联 CSS 只在内核启动时读盘——**改版后必须重启应用**（§57 经验），Page.reload 会造成新旧两套 CSS 混合态。

## 0.5.6 — 2026-09-09

月盘变形硬化：移除 `.pg-flatmoon` 容器的静态 `transform:translate(50%,-50%)`——静态 transform 同样把整棵月盘子树提升为合成层，是分数 DPR 下残留的最后一个光栅化错位风险源。改用无 transform 等价定位（`right:calc(min(880px,46vw)/-2)` + `margin-top` 负半程），位置像素级不变（盘心压宿主右缘、垂直居中，探针实测偏差 <1px）。至此月盘栈零 transform、零合成层，此类变形在构造上不可能再发生。

**排查附记**
- 用户回报「仍然有变形」时实例链路核查：内核按进程随机 nonce 生成 per-plugin rev、combo rev 为内容哈希（`framedHash("combo",[bytes,map])`）——重启即换 URL，理论上无陈旧代码路径；内核 `dsh-client-modules` 的 `rebuilt()`（HMR watch）是运行期唯一换 rev 的入口，宿主内联 CSS 只在内核启动时读取，**改版后必须重启内核/应用**，Page.reload 只能刷新 client.js 一侧（会造成新旧两套 CSS 并存的混合态）。
- 实测矩阵（均正圆）：活窗口真 GPU DPR 2.73 / 活窗口模拟 1.75 / 无头 2.73 / 无头 1.75。

**验证**
- 活窗口：容器 `transform:none`、`right=-323.685px`、`marginTop=-323.685px`、647.4×647.4 正圆、盘心压右缘、垂直居中。
- 回归全绿：verify-flat / verify-craters / verify-glyphs。

## 0.5.5 — 2026-09-08

月盘变形修复：月面公转从 transform 合成层动画改为 left 属性动画——分数 DPR>2（如 2.73）下合成器光栅化错位把月盘横向拉伸 ~1.2×（月坑变椭圆、盘缘变直线）。

**改动**
- 根因（无头 Chrome DPR 2.73 复现 + 活页面 A/B 实锤，非猜）：DOM 层 `.pg-flatmoon` 647.4×647.4 正圆、祖先链零变形 transform——CSS 无错，变形发生在合成器。注入 `.fm-strip{animation:none}` 月盘立刻恢复正圆 → 罪魁 = `palis-boot-pan` 的 `translateX` 关键帧产生的合成层，在分数 DPR>2 下被光栅化/合成错位。
- 修法：位移改 `left` 属性动画（主线程 layout 驱动，不产生合成层），各 DPR 几何一致。两处共用关键帧位移量不同（pb-moon 200% 宽需 -100% 容器，fm-strip 400% 宽需 -200% 容器）→ 拆分：`palis-boot-pan` 本体改 `{left:0→-100%}`，fm-strip 独立新关键帧 `palis-fm-pan{left:0→-200%}`（140s 不变）。RM 分支不受影响。
- 探针同步：`verify-flat.mjs` 的 fmAnim 断言更新为 `palis-fm-pan`。
- 新增复现/对照脚本：`verify-moon-ab.mjs`（DPR 2.73 无头 A/B 截图）。

**验证**
- 无头 DPR 2.73 复验：月盘/月坑正圆、盘缘平滑弧线（对照修复前 `ab-anim.png` 的椭圆坑+直线盘缘）。
- 桌面端活窗口（DPR 2.73）CDP 刷新后实测：`anim=palis-fm-pan`、`stripTransform=none`、647.4×647.4 正圆、截图目检月坑正圆。
- 回归全绿：verify-flat / verify-craters / verify-glyphs。

## 0.5.4 — 2026-09-08

内核 0.1.2-rc.1 兼容：适配 `dsh-settings` 删除的 `settingsNamespace()`（命名空间直传字符串）；老内核同样兼容。验证：0.1.2 预览内核全套回归 7/7 绿 + 截图目检。

**改动**
- `src/index.ts`：删 `settingsNamespace` 导入，`ns` 直接用 `SETTINGS_NS`。

## 0.5.3 — 2026-09-03

闪烁收尾：ping 环重尺寸排队到各环不可见窗口落笔——侧栏/面板切换的残留闪源根除。

**改动**
- 根因：§25 阈值门+冻结窗只压住 ping 重尺寸的**次数与时机窗口**，落笔相位仍随机——三环错相（0/2.13/4.27s，6.4s 循环）下任意时刻大概率有一环可见，落定后 ~1s 心跳落盘的那次重尺寸把无限动画插值顶爆 = 用户仍见的单帧爆亮。月球迁入星系中心（0.5.0）后环闪光贴着亮盘，存在感放大。
- 修法：`queuePingResize`——重尺寸不立即落笔，150ms 轮询各环 computed opacity，每环等到自己的 opacity≈0（循环 100%→0% 段）才改 width/margin。环透明时改尺寸对画面零影响；最坏等待 ≈ 一个循环，期间环径保持前值（直径差无感）。
- 附带修复：`removeSonar` 重置 `sonarLastPingD`——重挂载的全新 `<i>` 是 CSS 默认 760px，旧基数会让阈值门误判「增量 ≤40」永不定径（面板/会话切换后 ping 环偏小滞留的潜伏 bug）。

**验证**
- 新探针 `verify-ping-invisible.mjs`：两次收放共 6 次重尺寸（3 环 × 2），全部落在 opacity 0.015–0.02 的不可见窗口，resizeVisible=0；三环各自等到自己的窗口（t=1853/4107/6203 自然错开）。
- `verify-panel-switch.mjs`（新）：对话/轨迹 tab、工作区分组、会话项切换全程元素零置换、canvas 零重置——面板切换不重挂载层，排除一类嫌疑。
- 回归全绿：teleport（月球逐帧跟随零跳变）、craters（dx/dy=0）、flicker-mech（widthChanges=2）、glyphs（PASS）。
- 交付注记：正式桌面端需重启应用刷新（§49.2）。

— 署名：ox-alpha（2026-09-03）

## 0.5.2 — 2026-09-03

闪烁修复：侧栏收放落定帧的月球/环系瞬变回跳 + ping 重尺寸爆亮；平面月盘 140s 公转硬切。

**改动**
- 根因（verify-moon-teleport.mjs 取证，非猜）：v0.5.0 月球迁入星系中心后，§25 冻结窗「过渡期整体冻结、落定一次落盘」对又大又亮的月球变成单帧传送——探针实测落定帧月球 1184→1296（+112px）、盘径 579→636、ping 环 1737→1910 重尺寸（阈值门放行的一次爆亮）全压在同一帧。过渡期月球/环系还会相对轨道背景越漂越偏（背景随滚动体连续居中，声纳 style 冻结不动）。
- 修法：**冻结窗只冻 ping 重尺寸**（§25 定案防护原样），旋转环/度数/月球逐帧跟随滚动体几何——落定零跳变；orbitFrame 旋转冻结行同退（否则环停球转，新的不一致）。纯 transform/left/top 写入，无 canvas 位图重置、无动画插值风险。
- 平面月盘公转无缝化：单 img 宽 200% 时 translateX(-50%) 只走过半张图，循环点左/右半图内容不连续 = 每 140s 一帧硬切（0.4.9 潜伏）。改双副本条带 `.fm-strip`（宽 400%、各载一整张 2:1 贴图），位移一整张图宽 = 副本 B 精确顶替副本 A。
- 注释同步：theme-core 声纳头注释补登 `<b>`/g0/g1 退役（0.5.0/0.5.1 漏改）。

**验证**
- 新探针 `verify-moon-teleport.mjs`：修复后收/放全程月球逐帧平滑跟随（1408→1297→落定不动），声纳同步，ping 每次落定只重尺寸一次（§25 行为保留）。
- 回归全绿：craters PASS（dx/dy=0）、flicker-mech widthChanges=2、glyphs PASS、flat PASS（断言更新：动画改断 `.fm-strip` + 双副本 + 条带 4×盘径）。
- 交付注记：本轮含 CSS 迁动——正式桌面端需重启应用刷新 host 首帧快照（§49.2）。

— 署名：ox-alpha（2026-09-03）

## 0.5.1 — 2026-09-03

收尾：星系中心残留的圆点和一圈圆弧退役——粒子月球独占中心天体位。

**改动**
- 用户指令：「有了粒子月球，中间的圆点和一圈圆弧就可以去掉了」。chat 态截图中心残留的灰圆点+圆弧，真身有两处：
  - `ART_ORBIT` 背景 SVG 中心三件套：r=96 碟（fill .018+stroke .1）、r=30 圈（stroke .3）、r=10 圆点（fill .34）——烙在 `[data-conversation-scroll]` 的 CSS 背景里，chat 态消息间隙露出的就是它（v0.5.0 退役的 `<b>`/g0 只是 DOM 层那份，背景这份漏拆）；
  - 声纳旋转环 g1（r=96）：月盘半径 0.15·S 已覆过 r=96，环弧横穿月面。
- 三件套与 g1 全部拆除；`.palis-sonar s.g1` 基础规则 + activity 规则同删；声纳头注释更新（`<s>`×4：蓝环 r=184 + 灰环 r=348/264/430）；ping 只剩 `<i>`×3。

**验证**
- `verify-craters.mjs` PASS（中心 dx/dy=0、盘径 clamp 区间、粒子 29959、铭牌 overlap=false）；`verify-flicker-mech.mjs` widthChanges=2；`verify-glyphs.mjs` PASS（degs 归位）。
- 新建 `verify-chat-moon.mjs`（chat 态链路）：点侧栏既有会话进 active 态，探针 globe/sonar 圆心一致，截图 chat-moon-2560.png 确认中心圆点/圆弧消失、月盘居轨道系中心。
- 交付注记：本轮含 CSS 删除——正式桌面端需重启应用刷新 host 首帧快照（§49.2）。

— 署名：ox-alpha（2026-09-03）

## 0.5.0 — 2026-09-03

构图改版：粒子月球迁入声纳轨道系中心 = 星系的中心天体（替换原中心圆点）；月面铭牌迁上平面月盘。

**改动**
- 用户构图指令：左锚粒子月球与右缘平面月盘视觉比重冲突——月球移入星系（轨道环系）正中，「对那圆点进行替换」。
- 定位机制重写：`layoutGlobe` 与 `layoutSonar` 同圆心公式、同 S 基准、同冻结窗（ResizeObserver 逐帧跟随滚动体几何）；缩放走 transform（origin 0 0：`scale(k) translate(-50%,-50%)` 元素中心精确压圆心），1100px 固定内部几何整体等比——零重排、零位图重置、零 transition。盘径 `clamp(0.30·S, 220, 520)`：嵌进蓝环（r=184）以内、压过 r=96 虚线环。
- 退役：球体自带 HUD 几何层（准线/十字/细环/GROUND TRACK 尺）、卫星轨道环 + 卫星、live 代码读数（仪器语言由声纳环系接管）；声纳中心圆点 `<b>` + 中心环 g0（r=30）由月球接替；左右侧栏全收的「满月揭示」滑动机制（`data-palis-moon`）整体退役——月球常驻星系中心，无藏可揭。
- 月面铭牌迁上平面月盘（回归参考图「标题在月面上」构图）：落点 = 可视半盘中心（元素左四分之一位），常驻静态（揭示滑动不再需要）；白字压淡灰月面 + 静态 text-shadow 分离层。
- 排雷：`syncMoonReveal` 去掉属性写入后一度无条件冻结——启动时的首次调用把 ensureSonar 的初始布局打进冻结窗，声纳/月球永不定位（探针取证：degs left/top=null）。修复：恢复 full 状态变化检测（翻转才冻结/加速），不置任何属性。

**验证**
- `verify-craters.mjs` 换断言：月盘中心压星系圆心 dx=0 dy=0、盘径在 clamp 区间、粒子 29959、铭牌×hero 卡 overlap=false（PASS）；`verify-flat.mjs` PASS（月盘/铭牌/碰撞矩阵）；`verify-flicker-mech.mjs` widthChanges=2、`verify-motion.mjs` exit=0、`verify-glyphs.mjs` PASS（degs 归位，JANK 同噪声带）。
- 截图核对（verify-out/craters-3840.png）：粒子月盘居轨道系中心、hero 卡盖盘中腰（上下弧露出 = 行星晕），蓝环贴盘外缘；右侧平面月盘 + 铭牌成画。
- 交付注记：本轮含 CSS 迁动/删除——正式桌面端需重启应用刷新 host 首帧快照（§49.2）。

— 署名：ox-alpha（2026-09-03）

## 0.4.9 — 2026-09-03

平面月盘：开机舷窗同款平涂月面进入常驻构成——粒子月球（左）的平面对位，「双月对望」。

**改动**
- 新增 `.pg-flatmoon`（glyphs 层栈底，artwork 门控）：ART_MOON_MAP 数据 URI 复用（与粒子点云/开机舷窗同一张贴图，浏览器复用解码），右锚半露——盘心压宿主右缘（`translate(50%,-50%)`，镜像左侧粒子月球的左缘裁切机理）；尺寸 `min(880px,46vw)` 正圆。
- 贴图 2:1 等高铺满圆窗，横移 -50% = 经度回绕的无缝公转（复用 `palis-boot-pan` 关键帧，140s 缓速）；limb 渐暗 veil（径向罩 + 内阴影）让半盘没入背景；外环细圈（fm-ring）呼应球体 HUD 环语言。
- 零逐帧 JS：唯一动效是 transform 合成器通道的贴图平移；reduced-motion 静止；直角模式圆形豁免（同 globe/sonar 先例）。

**验证**
- `verify-flat.mjs` 新增平面月盘断言（就位/正圆/pan 动画/盘心压宿主右缘 ±40px）双视口 PASS，既有碰撞矩阵零撞；`verify-craters.mjs`（粒子 29959、铭牌 overlap=false）、`verify-flicker-mech.mjs` widthChanges=2、`verify-motion.mjs` exit=0、`verify-glyphs.mjs` PASS（JANK 同噪声带）。
- 截图双态核对：hero 默认态右缘平涂半盘（verify-out/flat-3840.png）；满月揭示态 = 左点云半盘 + 铭牌 / 右平涂半盘对望（verify-out/craters-3840.png）。

— 署名：ox-alpha（2026-09-03）

## 0.4.8 — 2026-09-02

构图改版：粒子月球移锚左缘、只露半盘；月面铭牌标题（PALIS 09A / 正在接入 PALIS 管理系统）随揭示态滑入。

**改动**
- 数据天体左锚：默认态左缘弧带（540px 元素切片，镜像原右弧）；满月揭示态（双侧栏全收）= 球心压左缘、右半盘成画（`translateX(-50%)`，「只露一半」）；hero 态镜像左藏。球内 HUD 微件（准线/十字/live 读数）全部镜像到元素右半 = 两态恒可见区。
- 新增月面铭牌（`.pg-moontitle`，挂进球体元素）：衬线宽字距大字 PALIS 09A + 副标「正在接入 PALIS 管理系统」（PALIS 钛蓝 accent），落点 = 可视半盘下三分之一区（元素 70%/68%），随 `data-palis-moon="full"` 从藏侧滑入（transform+opacity 合成器通道，与球体滑动同节奏）；默认态隐去不常驻。
- glyphs 层避碰：竖排幽灵大字退役（品牌位移交铭牌）；等高线地形碎片/单色色卡阶梯右迁（right:6.2%/2.4%）；PT-01/02 测点移出月盘区（24% 列）。
- **排雷**：host 首帧 CSS 是内核启动时快照——内核不重启则旧快照与 client 新表并存合并，迁向规则（left→right）会过约束失效（LTR 下 left 胜）。所有迁向规则补 `left:auto`/`right:auto` 显式复位（陈旧快照并存时也降级正确）；CSS 迁动类更新需重起内核/应用刷新首帧。

**验证**
- `verify-flat.mjs` PASS（碰撞矩阵换血：去 ghost、加 stream 邻对；双视口零撞，铭牌 accent 色断言）；`verify-craters.mjs` 满月截图 = 左缘半盘 + 铭牌成画（verify-out/craters-3840.png），新增铭牌 vs hero 输入卡重叠断言（overlap=false）；回归 flicker-mech widthChanges=2、motion exit=0、glyphs PASS。
- **修订（用户截图反馈）**：铭牌初版挂 glyphs 层居中 46%——hero 态被居中输入卡吞掉右半（「09A」/副标尾ghosting），且宿主侧栏偏移会推偏落点。修订：迁入球体元素（月面相对定位 + 罩层之上），下迁至可视半盘下三分之一区（70%/68%）双视口避卡（WORKLOG §49.5）。
- 帧预算：headless 软渲染 JANK avg 28.9→33.4ms——半盘藏于展开侧栏下的合成过绘开销（软渲染无 GPU 合成，高估真机）；JS 逐帧成本未变（WORKLOG §49）。

— 署名：ox-alpha（2026-09-02）

## 0.4.7 — 2026-09-02

环形山定向坡影：坑体从「平面环」读成「凹碗」——凹凸起伏感。

**改动**（纯贴图侧，引擎零改动零运行时成本）
- 诊断：旧配方是「均匀亮环 + 均匀坑底」的反照率画法——环处处等亮，眼睛读不到起伏。
- 重写 `craterArcs` 为三区坡影（太阳方位 SW≈217°，沿用旧明暗弧光源）：迎光 SW 亮弧（全收档致密亮粒 + 引擎 1.3× 提亮）、背光 NE 影弧（压进 <128 抽空带 = 粒子场咬出暗缺口）、坑碗 NE 偏移影核（双层暗斑）。
- 六族坑体适配：中坑/盆地叠坑/小坑×64/南半球群×40 的均匀亮环降为极淡结构环（保轮廓连续）+ 坡影弧组；年轻亮坑/射纹大坑主环 α .75/.72→.45 让位于方向性；小坑（r<18）影弧收窄减淡防淹没本体；链坑/微坑不作坡影（尺寸不够三区，保持颗粒噪点语义）。

**验证**
- 满月盘粒子 29959（影弧抽空带 -376）；截图坑体呈「亮月牙 + 暗缺口」凹陷读感，月海/高地宏平衡无虫蚀（verify-out/craters-3840.png）；`verify-flicker-mech.mjs` widthChanges=2、`verify-motion.mjs` exit=0、`verify-glyphs.mjs` PASS（JANK 与 0.4.6 同噪声带）（WORKLOG §48）。

— 署名：ox-alpha（2026-09-02）

## 0.4.6 — 2026-09-02

月面月坑加密：坑感由「贴图暗示」改为「粒子密度直接成形」。

**改动**
- 引擎侧：亮边带格点每格 3 颗哈希链确定性微抖子点（`PT_RIM_SUB`）——地形点云「一格一颗」本是密度上限，贴图再亮引擎每格也只吐一颗；破格后坑环/岸线/射纹从「格点虚线环」变「致密环带」。抖动幅度取半格步长恰铺满格间（再小会露出「三连珠」虫状纹理，实测修正）；点云上限 24000→32000（实测 30335，未触发抽稀）。
- 贴图侧：坑底填充 α .42/.4/.38→.30——高地坑内落入 0.78 密度档 = 疏粒盘，坑碗不再空洞（月海上的坑底仍抽空，海面保持干净）；坑环全面加粗提亮（主坑环宽 r*0.11→r*0.16，小坑/南半球群/链坑/微坑同步）。

**验证**
- 满月盘粒子 21236→30335，坑环密度肉眼提升、无过曝、月海空洞保留（verify-out/craters-3840.png）；帧预算 A/B 对照（子点开/关）avg 28.2/27.1ms，劣化在软渲染噪声带内；`verify-flicker-mech.mjs` widthChanges=2、`verify-motion.mjs` exit=0、`verify-glyphs.mjs` PASS（WORKLOG §47）。

— 署名：ox-alpha（2026-09-02）

## 0.4.5 — 2026-09-02

平面构成补位：粒子/点线之外，补「面」与「版式」类平面设计元素。

**新增**（均入 `.palis-glyphs` 层，artwork 门控，纯静态零 JS）
- 错位图版：实心面 + 描边面套版偏移叠印 + 45° 剖面纹带 + 铭牌（印刷套准语义，右上）；
- 单色色卡阶梯：6 灰阶递进 + 1 暖橙 accent 竖排色条 + 竖排标签（设计系统 swatch / 校色条语义，左 gutter 下段）；
- 栏栅格碎片：6 竖 + 2 横参考线网格只露一角（版式网格语义，repeating-gradient 一次成组，中右空带）。

**验证**
- `verify-flat.mjs`：双视口（3840×2058 / 1920×1080）三元素就位、灰阶递进、与既有元素矩形零碰撞（1920 下色卡横排标签曾撞等高线块，改竖排标签修复）；`verify-glyphs.mjs` 回归 PASS（WORKLOG §45）。

**注记**
- 本轮曾误判闪烁根因对月球 filter/声纳冻结窗动刀，经指正后全量回滚——侧栏收放闪烁的最终定案见 WORKLOG §25/§26/§31（真凶 = 声纳 ping 环逐帧重尺寸；冻结窗 + 阈值门双保险，勿拆）（WORKLOG §46）。

— 署名：ox-alpha（2026-09-02）

## 0.4.4 — 2026-09-02

美术构成扩充：打破「点 + 圆 + 横线」的单一构成。

**新增**
- `.palis-glyphs` 构成层（挂 [data-phase]，artwork 门控）：竖排幽灵大字 PALIS·09A（描边空心排版，会话列左 gutter）/ 等高线地形碎片（三峰偏心嵌套轮廓 + 测量十字 + TERRAIN 铭牌）/ 测量十字 + 编号坐标 ×6 / 右缘 hex 数据流（transform 无缝滚动，活动态提速，RM 静止）；
- 星尘星座连线：近邻星点（<110px、每星 ≤2 条）极淡连线，点阵升级为缓慢重构的网络构成，活动态提亮；
- 声纳方位读数：最外轨道环外缘 000°/090°/180°/270° 度数标记（layoutSonar 按 S 定位）。

**性能**
- 新元素全部静态或 transform 合成器动画，零逐帧 JS（星座连线循环实测 0.076ms/帧）。

**修复**
- 侧栏收放时星场频闪（v0.1.2 潜伏缺陷，被星座连线放大）：星场 ResizeObserver 在布局过渡期间逐帧清空画布并重撒全部星点随机坐标（实测一次收放 15 次重撒）。改为落定 160ms 防抖重置 + resize 不重撒（既有星保留、出界环绕回场、按面积调量）；连线改为跨帧持久配对 + 110/128px 建立/断开滞后带，消除逐帧贪心重选的整网生灭。修复后实测 widthChanges 15→2、各稳定段连线数逐样本恒定（WORKLOG §44）。

— 署名：ox-alpha（2026-09-02）

## 0.4.3 — 2026-08-31

声纳星系波纹降权：静默/活动态双维度收敛（ping 峰值/辉光/周期、旋转环透明度、
行星亮度全面回落；活动 boost 4×→2.2×），月球保持唯一主视觉，双动效不再互抢。

— 署名：ox-alpha（2026-08-31）

## 0.4.2 — 2026-08-26

状态栏信息密度提升。

**新增**
- SES:会话名段（chat 态蓝色显示，hero 隐藏）；
- MDL:模型名段（从 composer 模型选择器读取）。

— 署名：ox-alpha（2026-08-26）

## 0.4.1 — 2026-08-26

Markdown 内容主题化。

**新增**
- 代码块：暗底 + 蓝边 + 左蓝条 + 等宽字；
- 行内代码：蓝底暗字；
- 引用块：左侧蓝色粗条 + 斜体；
- 表格：方角 + 表头蓝底；
- 链接：虚线下划线 + hover 实线；
- HR：虚线；
- 列表：`>` prompt 前缀 + 蓝 marker。

**实现方式**
- 从 [data-chat-flow] 稳定锚点向下匹配 HTML 标签（不碰哈希类名）。

— 署名：ox-alpha（2026-08-26）

## 0.4.0 — 2026-08-26

流式动效层：内核 shimmer/sweep 的 PALIS 诠释。

**新增**
- [CLERK] 标签流式闪烁（background-clip:text 渐变扫过文字，PALIS 灰白→蓝）；
- [TOOL] 行流式扫描（120px 高亮条横扫，2.6s ease-out）；
- 仅流式时启用，非流式零开销。

**参考**
- 内核 turn-status-shimmer / reasoning-row-sweep 模式，PALIS 蓝/ASCII 诠释。

— 署名：ox-alpha（2026-08-26）

## 0.3.4 — 2026-08-26

细节打磨。

**新增**
- 侧栏右缘接触刻度线（展开时 7px 等距 tick）；
- 磁带播放头 ▼ 三角标（指向当前读位）。

**修复**
- playhead CSS 结构修正（background/will-change 回归主体声明）。

— 署名：ox-alpha（2026-08-26）

## 0.3.3 — 2026-08-26

ASCII 细节批。

**新增**
- Tool 结果块虚线框（施工围挡语义）；
- 状态栏版本段末尾终端光标闪烁；
- Hero 欢迎屏坐标网格（56px 蓝点阵）；
- 状态栏 LN/CHR 字符计数器（输入实时更新）。

— 署名：ox-alpha（2026-08-26）

## 0.3.2 — 2026-08-25

磁带走带系统 + Hero 任务铭牌。

**新增**
- 磁带尺播放头：滚动时蓝色游标滑到滚动比例处（DOM+transform 合成器移动）；
- 状态栏 SCROLL 段叙事化：TAPE//START → 百分比仪表 → TAPE//END（蓝色高亮）；
- Hero 任务铭牌 EXPEDITION // 09A — ARCHIVE OBSERVATION（居中顶部）；
- 会话头双线规（底部主线+微光副线）。

**修复**
- 播放头原用 CSS 变量写在滚动容器上=全子树样式重算=卡顿——改 DOM+transform。

— 署名：ox-alpha（2026-08-25）

## 0.3.1 — 2026-08-25

tmux 式底部状态栏。

**新增**
- 底部 22px 状态栏：▲PALIS 09A 品牌段 / PHASE 相位段 / UTC 真时钟 / SCROLL 深度仪表条 / ●LIVE ○IDLE 活动指示（流式时红字闪烁+品牌段变红）/ ARCHIVE TERMINAL · REV 09A 版本段；
- 会话滚动容器底部让位 26px；面板 titlebar 加 REV·09A 铭牌。

— 署名：ox-alpha（2026-08-25）

## 0.3.0 — 2026-08-25

ASCII 取景框系统 + 细节打磨批。

**新增**
- 取景框层 .palis-frame：四角 L 裁切标记 + SYS//09A-C2 / ARCHIVE TERMINAL / REC 铭牌 + 左下 SCROLL 深度读数（ascii 仪表条）；
- 会话区磁带尺（顶部刻度带 + TAPE START 铭牌）；
- Composer IN//09A 标签耳 + 四角 L 括号；
- 球体地面轨道尺（盘底刻度带 + GROUND TRACK 铭牌）；
- 开机自检新增 GROUND TRACK LOCKED / VIEWFINDER ARMED 仪表行；
- 满月揭示曝光增益（brightness 1.6 + contrast 1.1）——整盘居中时月面点云清晰可读。

**验证**
- 快速连点 4 次 toggle：零尖峰；卡顿表 avg 9ms / max 24ms / 零掉帧。

— 署名：ox-alpha（2026-08-25）

## 0.2.1 — 2026-08-25

安全修复。

**修复**
- 回环校验从 Host 头（可伪造）改为 socket 对端地址：127.0.0.1/::1/::ffff:127.0.0.1。

— 署名：ox-alpha（2026-08-25）

## 0.2.0 — 2026-08-25

架构审查修正。

**修复**
- CRT 扫描频带层生命周期并入 ensure* 家族（主题禁用即摘除，此前残留 DOM）；
- 客户端 revision 轮询（2s）：外壳切皮肤/其它标签页的面板写入即时同步到已打开
  页面（此前仅启动时读取一次，需重载才生效）。

— 署名：ox-alpha（2026-08-25）

## 0.1.9 — 2026-08-25

月球旋转衔接优化。

**优化**
- 画布冻结退役（防闪已被声纳根治，冻结造成的旋转停顿一并消除）；
- 滑动旋转增强：侧栏收放时月球自转短暂 +3×（~1.2s 缓出衰减），过渡自带甩动感。

— 署名：ox-alpha（2026-08-25）

## 0.1.8 — 2026-08-25

卡顿根治：CRT 扫描频带拆独立层（合成器友好）。

**修复**
- 全屏 background-position 无限动画（palis-crt-sweep 11s）改为独立固定层 +
  transform 动画——旧实现全视口逐帧重绘，与所有过渡动画持续抢帧（卡顿元凶）；
- 量化验证：3 连点 toggle 214 帧平均 8ms、最大 24ms、零掉帧；闪回归零尖峰。

— 署名：ox-alpha（2026-08-25）

## 0.1.7 — 2026-08-25

过渡动画顺滑化。

**优化**
- 主题级缓动令牌换标准柔和曲线（cubic-bezier(.4,0,.15,1)），慢时 .24s→.3s；
- 月球滑入加 100ms 起步延迟（等布局过渡猛冲段过去，安静帧上滑）；滑出保持即时。
- 实测：滑动 25 帧、单帧最大步进 108px、无 >110px 突兀步进。

— 署名：ox-alpha（2026-08-25）

## 0.1.6 — 2026-08-25

信号门控与帧预算优化。

**优化**
- 声波振幅突变计数加流式门控：侧栏收放等结构性突变不再误抬波动条振幅；
- 星场隔帧重绘（30fps）：减轻主线程负担，布局/滑动动画帧更稳。

— 署名：ox-alpha（2026-08-25）

## 0.1.5 — 2026-08-25

恢复月球滑动过渡（直接 transform 状态值）+ 声纳 ping 环阈值门（闪的最终根治）。

**修复**
- 月球揭示/隐藏恢复滑动（.24s）；transform 改直接状态值（不再 var 组合）——
  组合值插值步进问题一并消除；
- 声纳 ping 环构建期定型 + 增量 >40px 才重尺寸：被逐帧改样式的动画环是
  单帧亮闪的最终根因，阈值门机制性根除（v0.1.4 的 700ms 冻结窗保留为双保险）。
- 验证：快速连点 4 次 toggle 角落尖峰数 0；滑动曲线平滑。

— 署名：ox-alpha（2026-08-25）

## 0.1.4 — 2026-08-25

侧边栏收放「闪」最终定案：元凶为声纳逐帧重布局，非球体。

**修复**
- **声纳过渡期冻结**（真正根治）：.palis-sonar 的 ResizeObserver 在侧栏
  布局过渡期逐帧重设几何（ping 环 width/margin），把 palis-sonar-ping 动画
  插值顶到最坏帧=单帧整盘爆亮（左栏→中间上下闪、右栏→右下角闪）。冻结窗
  700ms（与月球换牌同钩子）：layoutSonar 直接返回、orbitFrame 不落盘。
  实测快速连点 4 次 toggle 角落尖峰数 0。
- 球体侧同步完成：will-change 常驻 + 揭示改「隐身换位交叉淡化」（纯 opacity、
  零位移，位置变化永无 transform 动画）。

— 署名：ox-alpha（2026-08-25）

## 0.1.3 — 2026-08-25

满月揭示「闪烁」根治：揭示滑动与布局大换血解耦（延迟落定后滑入，快攻缓收不变）。

**修复**
- **单帧亮闪（月球揭示）**：实机 screencast 事件流捕获到揭示过渡瞬间一帧全屏
  亮度突刺（+8.7），帧对比定位为月球在布局列宽过渡同窗口滑动时的合成竞争
  （子层/画布晚一帧上图=裸亮帧）。双修复：
  - `.palis-globe` 加 **`will-change: transform` 常驻**——合成层在动画前就存在，
    消灭中途提升的掉队帧（与 better-sidebar 面板同款层提升修复模式）;
  - **揭示方向（滑入）挂同长 transition-delay**——月球等布局落定后在安静帧上滑，
    与侧栏收放的大换血完全错峰；隐藏方向不延迟（开侧栏立即收月）。
- 验证：修复后全窗口亮度曲线零突刺；月球揭示/隐藏行为保持；左栏稳定性回归通过。

— 署名：ox-alpha（2026-08-25）

# 更新日志

## 0.1.2 — 2026-08-23

月球天体（粒子点云引擎）+ 开机动画改版 + 签名瞬间批：右侧天体地球换月，
开机 CRT 点火 + 舷窗月球，CRT 电源闪屏、快捷键、预设与星尘漂移场。

**新增**
- 数据天体 → **月面点云**（引擎路线经用户实机裁决反转：粒子版常驻，逐像素
  位图引擎退役）：构建期按经纬网格（0.7° 步长）采样月面贴图 R 通道亮度 →
  **约 2.1 万颗地形粒子**组成旋转点云（`window.__palisPoints` 探针实测 20923）；
  **密度分档采样**（海报半调逻辑）：亮度带 → 离散 keep 概率三档密度
  [56:0][128:0][152:.78][256:1]，档边界卡在实测直方图谷底与岸线起点
  （128 = 月海峰 ~85 与高地峰 ~167 之间的真空带；152 = 岸线亮边主体起点，
  实测岸线高亮 R 集中 144-175）——**月海彻底抽空（空洞 = 粒子的绝对缺席），
  岸线/亮坡一档全收并提亮 1.3×，月海空洞由「无/有」一刀切开、边缘自然显出
  致密亮边**；正交投影
  后半球剔除，深度调 alpha/尺寸，alpha 分桶批量绘制 + additive 加法混合微光，
  全收档 ~1/3 钛蓝着色（矿质色落到亮边带）+ 中亮带 ~3.4% 蓝火花。
  卫星/HUD 环/live 读数/DOM 尘埃层不变
- 月面贴图 `ART_MOON_MAP` 程序生成器（等距圆柱 2048×1024 横向无缝，确定性
  PRNG seed 20260823）：**月海十二片按真月 selenographic 坐标排布**（冷海北极
  长带/雨海/澄海/静海/危海/丰富海/酒海/云海/湿海/风暴洋/格里马尔迪/柏拉图，
  背面纯高地）——月面「人脸」构图成立；银色高地 = 中灰基底 + 6 倍频反照率湍流
  斑驳（.5 强度）；月海 disloc 位移破边 + 同位移场亮岸线 + 海内皱脊/月溪/海缘
  幽灵坑；环形山谱系——远古大盆地双环 ×2 + 中坑 ×16 + 小坑 ×64（平方坑径谱）+
  链坑七连珠 ×2 + 南半球饱和坑群 ×40 + 微坑颗粒 ×240；年轻亮坑 ×5 带径向渐隐
  喷发毯；**射纹系统仿真位**：第谷居南半球 24 条楔形射纹横跨半盘、哥白尼、
  开普勒；Aristarchus 蓝白最亮斑（呼应蓝色点缀）；**矿质双色调罩层**（高地
  暖铜薄罩 + 月海钛蓝薄罩，引擎 R 通道采样下同薄罩还微调点云密度）；**三档半调
  网点**（高地细粒 5px + 中粒 26px 经反照率明度蒙版 = 真半调 + 海内粗粒 20px）；
  极区微亮 + 高频颗粒收尾——自转一周讲完月球两面
- **开机动画改版**：CRT 点火入场（clip-path 水平亮线展开 + 亮度闪光，.55s）→
  380px 圆形舷窗内月面贴图 60s 横向平移（舷窗里转动的月球，呼应 PALIS 09A
  参考屏大圆窗），标题/副标叠视窗中央，进度条 + 6 行自检日志保持；覆盖层
  自带扫描线 + 暗角
- 星尘漂移场 `.palis-starfield`：全屏 80–240 颗微粒缓漂 + 异相闪烁（冷灰白主体 +
  蓝火花 + 暖橙点缀），独立轻 rAF，reduced-motion 单帧静态
- **CRT 开关机闪屏**：POWER 切换时水平亮线展开/收束的通断电动画（样式在常驻
  PANEL_CSS——断电播放时主题表已摘除；首帧加载不播，boot 序列负责）
- **Ctrl+Alt+P 快捷开关**（AltGraph 放行防 AltGr 输入误触）+ 状态行操作提示
- 面板 **PRESET 预设行**：CRT·MAX / TERMINAL / BARE 一键组合写入（逐字段走统一
  写路径，revision 守卫，quiet 汇总日志）
- live 读数 UTC 真时钟（每拍走秒）
- **满月揭示**：左右侧边栏都收起时粒子满月整盘滑进视野（半弧 540px ↔ 满月
  60px 露出，用 `translateX` + `--moon-x` 表达、过渡挂全局节奏令牌，与侧栏
  同相运动）；左侧栏看内核布局框架的
  `data-sidebar-collapsed` 数据属性、右侧栏看 `<html>` 的 `--dsh-sidebar-width`
  布局变量（0px/未设置 = 收起）——只信稳定契约，不碰哈希类名；任一栏展开即收回半弧
- **CRT 三层过渡化**：噪点/扫描线/暗角改常驻层 + opacity × 开关变量门控
  （on/off 渐隐渐现，不再瞬灭瞬现）；三档浓度变量注册 `@property <number>` 挂进
  html transition——intensity 档位切换时浓度平滑插值（变量渐变会带进 background
  rgba 里的 var() 引用）；标题辉光加 text-shadow 过渡；不支持 @property 的引擎
  静默退回瞬时切换，零回归
- **外壳皮肤联动路由** `/api/palis-theme`：实现桌面外壳 pushThemeToKernel 契约
  （原为缺失的内置扁平插件预留，启动 push 一直 404）——外壳皮肤切入 palis 即开
  主题、切出即关主题；GET 返回当前主题态（兼容轮询语义）；内核 ready 时外壳
  自动同步当前皮肤

**调整**
- 球径随视口缩放：`min(1500px, max(1100px, 56vw))`——1920 及以下保持 1100 原样，
  3840 超宽 → 1500px（粒子同比例变粗，蔓延感不随视口稀释）；JS 引擎仍按 1100
  坐标系布局，CSS 整体放大，内部常数零改动
- reduced-motion 完整治理：球体点云静帧化（渲染一帧 + 写一次读数，循环不启动）、
  星场单帧、boot 自检跳过
- 直角模式豁免扩列：开机舷窗 `.pb-port` / `.pb-port-ring` 加入
  `border-radius:50% !important` 豁免（与 globe/sonar 同例——具象图形非 UI 铬件）

**修复**
- 注入器预检正则按 `register({…name` 扫描：注册调用保持单行排版（多行排版被误判
  坏骨架）；源码注释避开 `slots.register(` 字样

## 0.1.1 — 2026-08-22

声线波动条 + 声纳扩散：composer 顶蓝条与背景轨道图中心徽记，双双随 AI 思考/输出呼吸。

**新增**
- 输入区顶边 2px 蓝条 → 声线波动条：`[data-composer-card]` 顶边叠加 canvas，
  依内核语义属性 `[data-streaming]`（assistant 消息根在推理/输出全程置位）驱动——
  静默时画布清空（原蓝边 = 静止基线，观感与 0.1.0 完全一致），
  活动时画复合正弦波：基础振幅 + 输出突发密度加成（思考缓涌、输出起舞），
  快起慢落 + 700ms 软着陆保持，跨 thinking→output 间隙不闪断
- 轨道图中心徽记 → 深空声纳：`.palis-sonar` DOM 层按背景定位公式反解圆心对位
  （ART_ORBIT 是 CSS 背景，本身动不了），三环错峰扩散（最大 760px，越过最外轨道环）
  + 中心蓝点脉动（静默也缓慢呼吸）；与波动条共用同一活动门
  （`html[data-palis-activity]`）——静默 = 6.4s 深空慢 ping，活动 = 2.3s 密集快 ping
  且峰值更亮，两态经 `data-streaming` 门闩切换
- 轨道环全层联动：`.palis-sonar s`×5 与各静态轨道环同径的 mask 虚线环
  （蓝 r=184 + 灰 r=348/264/96/30，厚度与虚线节奏按各环 SVG 参数折算）盖在静态环上
  = 原环转了起来；角度由 JS 逐帧积分（ω = speed·(sin+0.6·sin+0.3) 双频叠加，
  符号自然翻转 = 不规律顺/逆时针交替），大环慢、小环快
- 行星公转：ART_ORBIT 抠掉的 8 个节点白点由 `.palis-sonar u` 接替——正交 4 颗巡 r=430、
  对角 4 颗巡 r=294（原角位作初相，开普勒式内快外慢），另加 1 颗 accent 卫星巡蓝环；
  静默 4 分钟/圈持续慢转（独立 rAF，不随波动条引擎休眠），活动门把全部角速度 ×(1+3·heat)
- 数据天体卫星呼应：`.palis-globe` 新增卫星层——accent 卫星沿贴 r1 HUD 环半径的倾斜轨道
  公转（周期 = 球自转的 1/5，方向与球面漂移一致 = 前半球右→左），轨道面缓慢进动（240s）；
  真 3D 遮挡：后半程落进球盘半径即被遮蔽淡出（盘外仅压暗），近大远小（scale ±22%）；
  球自转、卫星公转、进动共用活动门 boost ×(1+2·heat)——AI 工作时整个天体系统一起加速
- 两件动效均遵守 `prefers-reduced-motion`（减免时不动工，保留静态蓝条/轨道图）

**调整**（实机反馈：静态中心抢视线，动效不够明显）
- 压暗静态中心：轨道图中心三件套——r=96 圆盘 fill .04→.018/stroke .16→.10、
  r=30 中心环 stroke .42→.30、中心点 fill .45→.34，蓝环描边 .34→.30
- 提亮全部动效：ping 峰值 .45→.58/.85→.95（描边 .6→.7、光晕 26px→30px）、中心蓝点
  静默 .3→.5/活动 .85→.95、五档旋转环静默→.38~.6/活动→.58~.95、
  行星 5px→6px 且静默 .75→.92、卫星轨道环 .4→.52/.62→.78、卫星前半程 .95→1

**细节打磨**（持续实机反馈）
- 最外圈 r=430 也有旋转覆盖环（g4，整套六环全转）；ping 扩散环定径改为
  max(760px, 1.1·S)——超宽屏下也保证越过最外轨道环
- 卫星轨道环 DOM 层序移到球体之下：掠过球盘的弧段被球遮蔽，与卫星的遮挡逻辑一致
- HUD 代码读数 live 化（原 ::before/::after 死文字 → globe 引擎 500ms 覆写）：
  LON = 球面中央经线，随自转真实走动；SIGNAL 随机游走、活动时降到个位数 ms；
  TRACK 10 OBJECTS = 9 行星 + 1 卫星的真实数量；INDEX 末位 hex 持续跳动
- 球面质感重写：最近邻采样 → 双线性采样（x 向位与环绕，块状锯齿消除）；
  新增逐像素方向光照（固定光源屏幕左上前方，昼 .92/夜 .2，昼夜分明——太阳不随球转）、
  临边大气辉光（(1-nz)³ 偏蓝）与 ~6px 柔和临边（alpha 渐隐替代硬切边）；
  静态几何/光照/辉光全部初始化预计算，渲染循环零三角函数；
  CSS 遮光层相应减负（去掉与引擎光照打架的重径向暗化，只留印刷轻渐变）
- 质感重心调整为构成元素（实机反馈：不要强光照）：光照降为微弱体积暗示
  （.76~.92，对比 1.13×）、临边辉光减到勾边级；贴图构成元素全面加强——
  等高线 6→9 条且 .10→.17、经纬网 .04→.07、半调网点 r.6→.7/.07→.09、
  陆块阈值阶梯 .9→1、云层 .26→.3，新增 8 个测量十字 + 2 个标记环（数据刻度，
  随球面卷曲旋转）；表面粒子 1 层 14 点 → 3 层 33 点异相闪烁（含蓝色火花层）
- 去光照去辉光（实机反馈：球太亮，只留纹理质感）：逐像素方向光照与临边大气
  辉光整体删除，渲染改固定曝光 ×0.55（实测画面亮度 161 → 96，昼夜比 1.13× →
  1.01×、临边蓝偏置 +21 → 0）；双线性采样、~6px alpha 柔边、全部贴图构成
  元素与三层表面粒子保持不变——球面回归纯纹理灰度
- 去柔和临边（实机反馈）：alpha 渐隐预计算整体删除，圆盘像素 alpha 一律 255，
  硬切边（视觉边缘由球形容器的圆形裁剪 + 细描边提供）
- 深压暗（实机反馈：还是太光亮，对标早期暗色观感）：固定曝光 ×0.55 → ×0.32
  （实测画面亮度 96 → 56）；贴图线元素按低曝光补强保持可读——等高线 .17→.26、
  经纬网 .07→.12、半调网点 .09→.14、数据刻度 .24→.36
- 多尺度网点体系（参考 PALIS 总目录屏的密度搭配）：单层 4px 网点 → 四层——
  细粒 3px 底噪（.07）、中粒 4px（.14，原层）、粗疏粒 9px 暗区散布（.10）、
  陆块密度调制层 5px（.22，以陆块湍流明度×阶梯 alpha 作 mask——陆地网点加密、
  海洋稀疏，密度跟明度走）
- 纹理质感继续加强（实机反馈）：陆块梯田化（阶梯 6 档 → 7 档，地形台阶更碎）、
  等高线 9 → 13 条且位移 60→85 更锯齿、新增 8 条纵向波纹线（与等高线正交
  编织，纵向线不跨横向接缝天然无缝）、数据刻度 8 → 12 十字 + 3 标记环、
  噪点层 .08→.15；并修正"线叠在亮陆块上被冲淡"的对比衰减——等高线 .26→.3、
  波纹线 .16→.2、中粒网点 .14→.18、陆块调制网点 .22→.3
- 表面纹理对比加强（实机反馈）：渲染循环新增绕中灰 128 的对比拉伸（×1.4，
  写入 uint8 自动截断）——暗部更暗、亮部更亮、均值近似不变，不抬整体亮度；
  实测纹理能量 131~141 → 177（+30%），昼侧亮度 58 → 64 仍远低于压暗线
- 动效节奏令牌化（窗口切换衔接优化）：主题开启时覆盖内核过渡节奏
  `--ds-transition-duration .16s / -fast .08s / -slow .24s` +
  `--ds-ease-in-out: cubic-bezier(.3,.85,.25,1)`（缓动曲线更脆、起停更利落）——
  左右侧边栏、底部面板及一切挂这组内核令牌的控件一处覆盖全局同步；
  配套 dsh-better-sidebar 0.12.2 的面板交叉淡化/开关态指示（见其条目）
- 波动条 resize 落定防抖（侧栏开合闪烁治理，与 better-sidebar 同轮）：
  侧栏推拉/窗口缩放期间 ResizeObserver 逐帧触发，`canvas.width=` 赋值按 spec
  清空画布 = 蓝线闪断；新增落定防抖（落定 160ms 无新尺寸才重置画布，首次挂载
  仍立即），动画窗口内画布零清空（实测 backingChanges 0）

**修复**
- canvas 是替换元素：绝对定位 `left+right` 拉伸对其无效（固有尺寸胜出，实测画布塌成 36px
  只剩左端包络），`.palis-wave` 必须显式 `width: calc(100% + 2px)`
- 声纳环被直角模式切成方环：`data-palis-square="on"` 的全局 `border-radius:0 !important`
  波及 `.palis-sonar i/b`——按更高特异性豁免回 50%（光晕跟随 border-radius，一并复圆）；
  本轮行星点 `u` 一并加入豁免
- 旋转环定径偏小 6%：上一版按 0.368·S 直取 r/1000，漏算 mask 圆半径只占元素边长 47%——
  正确边长 = S·r/470，本轮起全部按此精解（旋转环与静态环严格同径）
- 数据天体被直角模式切方：全局 `border-radius:0 !important` 同样波及 `.palis-globe-sphere`
  与 HUD 环 r1/r2（球形容器变方盒）——具象图形非 UI 铬件，按声纳环同例豁免复圆
- 设置面板样式整体丢失（实机反馈，根因确凿）：cordis effect 重跑时新回调可能先于
  旧清理执行——旧实现 `if (panelTag !== null) return` 守卫让回调跳过创建，随后
  旧清理把唯一 tag 移除 = 面板永久无样式（实测 floatBtn 在而 panelTag 不在）。
  正确逻辑覆盖：`ensurePanelCss` 改按 DOM 实况自愈对齐（getElementById 为准，
  不信模块变量）；effect 清理函数不再删除 panelTag——PANEL_CSS 与主题开关无关、
  页面生命周期内常驻；Panel 组件挂载时自愈调用兜底
- 设置页窄列下开关折行断字：设置页 main 列实测仅 564px，`.ptp-toggles` 两列
  各 110px < 标签所需 ~134px——改 `repeat(auto-fill,minmax(150px,1fr))` 自适应减列
  + 标签 nowrap；STYLE 区通栏（`.ptp-cell-wide`，顺手消灭三 cell 两列的右下空洞）
- POWER/分段/浮动按钮点击后残留白色聚焦框：`:focus:not(:focus-visible)` 去框，
  `:focus-visible` 统一主题蓝框（键盘可达性保留）

## 0.1.0 — 2026-08-21

首个正式版本：PALIS 档案终端深度换肤（可开关主题面板）。

**新增**
- 设置面板「PALIS 主题」（`settings.section`，React 契约）：POWER 总开关、CRT 强度（LOW/MID/HIGH）、FX 层（扫描线/噪点/暗角/辉光）、STYLE 层（等宽/直角/角色标签/开机自检/背景图形）、自检日志（revision 可见）
- 右下角浮动快捷开关（一键接入/断开）
- 深度换肤：内核设计令牌全覆盖（78 别名 + 静态色板 → 黑白 + 蓝 #2b5fd9 + 红 #c8322b）、直角、等宽字体、方形滚动条、蓝底选区
- CRT 质感：feTurbulence 噪点、4px 周期扫描线 + 11s 慢速刷新带、暗角、屏幕边框罩
- 语义组件：消息流 `[CLERK]` / `[TOOL]` 标签、终端风输入区、Win95 式会话标题条、淡蓝/灰消息轨道
- 背景图形：**数据天体**（canvas 正交投影自转引擎 + 有机地形 + 等高线 + 半调网点 + 表面粒子）+ HUD 几何层（细实线轨道环/十字准线/代码读数）+ 环形轨道图 + 星尘
- 开机自检动画（PALIS 09A · 正在接入 PALIS 管理系统，typewriter 自检行）
- 首帧零闪烁：`webserver/index-inject` 注入 `PALIS_CSS` + boot 脚本（主题开启时）
- 持久化：`palis-theme` 设置命名空间（settings.yaml），409 冲突自动回读

**修复**
- 输入框文字不可见：内核用渐变文字（`-webkit-text-fill-color: transparent`），补 `-webkit-text-fill-color` 覆盖
- settings.section 挂载失败：按本内核 slots 契约改 `register(options, ReactComponent)`
- 天体自转实现三易其稿：贴图横移（圆柱感）→ CSS 3D 面片（放大露棱）→ canvas 正交投影（最终）

**依赖**
- host：cordis ≥4-rc、schemastery ^3.18、@deepseek-ai/dsh-settings（运行时来自桌面版 runtime）
- client：react（shell 提供）、@deepseek-ai/dsh-client-ui-slots

## 0.0.1 — 2026-08-21

- dev_scaffold_plugin 生成 ui-panel 形态骨架（host tsc + client tsdown，自包含 build.sh，不依赖 npm）
